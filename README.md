
  # NAGAE Studio Portal

  This is the official codebase for NAGAE Studio Portal.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  